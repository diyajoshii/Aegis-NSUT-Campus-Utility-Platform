// types/Bookmark.ts
export interface Bookmarks {
    _id: string; // Assuming _id is a string
    userId: string; // Assuming userId is a string
    schemeId: number; // Assuming schemeId is a number
    createdAt: Date; // Assuming createdAt is a Date
}